import { FilterData } from "../../shared/AppFilter";

export const initialUploadsFilterData: FilterData = {
  name: {
    selected: "all admins",
    options: ["all admins"],
    placeholder: "Name",
  },
};